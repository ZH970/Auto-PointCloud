# PointCloud 工具箱（ui.py）功能性使用文档

本工具箱是一个三页签前端，分别封装三段流程：

1. **TF/SD 卡导入点云到本地并可重命名**（main.py）
2. **Lidar ME 软件自动化处理**（auto.py）
3. **读取 points.csv 写入 Excel 数据表**（points2SLAM_Output.py）

前端通过**环境变量**把每个输入框的值传给对应脚本（例如 `SPC_DST`、`SPC_DO_IMPORT` 等）。点击“保存配置”会写入 ui_config.json，下次打开自动回填。

> 注意（来自项目约束）：  
> - 由于 LidarME 权限限制，通常需要**管理员身份运行**。  
> - 需要插上对应的加密狗/密钥后再启动软件（否则软件可能异常）。  

---

## 通用按钮/区域说明（所有页签通用）

- **运行 xxx.py**：启动对应脚本，并把当前界面参数写入环境变量后运行。
- **停止 xxx.py**：终止对应脚本进程（先 terminate，2 秒未退出则 kill）。
- **输出日志**：显示脚本标准输出/错误输出的合并内容。
- **保存配置**：保存所有页签当前参数到 ui_config.json。

---

# 1）TF卡导入（main.py）页签：每项含义

用于从 TF/SD 卡（通过**卷标**识别）筛选点云文件夹并复制到本地目标目录，复制后可弹窗重命名，并在目标目录下创建 `after/csv/<最终目录名>-` 等结构。

### 卷标 `SRC_LABEL`
- **含义**：Windows 资源管理器里 TF/SD 卡显示的“卷标名称”，例如 `LidarME`。
- **作用**：脚本会遍历 A:~Z: 找到卷标匹配的盘符作为源盘。

### 子路径 `SRC_SUBPATH`
- **含义**：源盘下的子目录路径（相对路径）。
- **用法**：  
  - 留空：检查源盘根目录  
  - 非空：只在该子路径下找点云文件夹

### 目标目录 `DST`
- **含义**：复制到本地的根目录（工作目录）。
- **示例结构**：
  - `DST\2025-11-11_15-50-12\...`
  - `DST\after\csv\<folder>-`
  - `DST\after\img\...`（后续由 auto 生成）

### `MIN_MB` / `MAX_MB`
- **含义**：按文件夹大小过滤（MB）。
- **作用**：只有在范围内的点云文件夹才会被复制。

### `REQUIRED_MOD_TIME(0/1/2)`
- **含义**：按“目录最后修改日期”筛选模式  
  - `0`：不按修改日期过滤  
  - `1`：启用过滤，并使用 `REQUIRED_MOD_DATE`（会弹窗可调整）  
  - `2`：只匹配最近 7 天内修改的目录

### `REQUIRED_MOD_DATE(YYYY-MM-DD)`
- **含义**：当 `REQUIRED_MOD_TIME=1` 时生效的日期。
- **格式**：`2026-01-29`

### `NoCopyFolder(0/1)`（对应 `DRY_RUN`）
- **含义**：干运行开关  
  - `0`：正常复制  
  - `1`：只打印匹配信息，不执行复制/重命名

### `VERBOSE(0/1)`
- **含义**：是否输出更详细日志。

---

# 2）LidarME 自动化（auto.py）页签：每项含义

用于驱动 Lidar ME UI 完成导入、坐标转换、导出等流程；其中“截图/选点”等仍包含人工步骤（按你当前脚本实现）。

### 点云根目录 `ROOT_STR`
- **含义**：点云工作目录根路径（通常与 `DST` 相同）。
- **脚本依赖**：  
  - 点云文件夹一般是四位数字目录：`ROOT_STR\1172`  
  - 标记目录：`ROOT_STR\after\csv\1172-`、`ROOT_STR\after\img\1172.png`

### LidarME 程序路径 `APP_EXE`
- **含义**：Lidar ME 可执行文件路径，例如：  
  `C:\Program Files\Lidar-ME\Lidar ME\Lidar ME.exe`
- **作用**：当检测不到已运行的 Lidar ME 窗口时，用此路径启动软件。

---

## 待处理来源（自动识别 / 手动输入）

### 自动识别（扫描今天的四位数文件夹）`SPC_AUTO_PENDING`
- **勾选**：自动扫描 `ROOT_STR` 下“今天修改过”的四位数目录，且未在 `after` 中标记为已处理的目录会被加入 pending。
- **不勾选**：进入手动模式，pending 由下面输入框提供。

### 手动输入 pending `SPC_PENDING`
- **含义**：指定要处理的文件夹列表。
- **格式**（任一即可）：
  - `1172,1173`
  - 多行输入（用空格/逗号/分号/换行分隔）
  - 也可填绝对路径：`C:\...\1172`
- **注意**：当勾选“自动识别”时，该输入框会被禁用（不参与）。

---

## 导入步骤（可选执行）

### 执行导入（新建任务/选择文件夹/RTK/确认）`SPC_DO_IMPORT`
- **勾选（=1）**：执行“导入步骤”，即对 pending 中每个文件夹：
  - 新建任务
  - 选择文件夹（指向该点云目录）
  - RTK 校准
  - 确认
- **不勾选（=0）**：跳过导入步骤，直接在任务列表中查找同名任务并进入处理。  
  适用场景：任务已经在 Lidar ME 里导入过了，只想重复跑后续流程。

---

# 3）写入Excel（points2SLAM_Output.py）页签：每项含义

用于读取一个 `points.csv`，提取两组点（或成对数据），写入指定 Excel 的某个 Sheet，并可另存为新文件。

### points.csv 路径 `SPC_CSV`
- **含义**：要写入 Excel 的 `points.csv` 文件路径。
- **来源**：通常由 auto.py 导出到 `ROOT_STR\after\csv\<folder>-\points.csv`。

### Excel 工作簿路径 `SPC_WORKBOOK`
- **含义**：模板或目标 Excel 文件路径（`.xlsx`）。

### Sheet 名称 `SPC_SHEET`
- **含义**：写入的工作表名称。
- **默认**：`SLAM扫描测试`

### 另存为(可空) `SPC_SAVE_AS`
- **含义**：输出另存为路径（可选）。  
  - 留空：通常表示直接写回原工作簿（取决于脚本参数实现）  
  - 不为空：写入后保存为新文件

### 保留图片列(默认1) `SPC_PRESERVE_IMAGE_COLUMN`
- **含义**：图片相关列的保留/处理策略参数（脚本内部用来决定图片列/位置的逻辑）。  
- **默认**：`1`

---

## 推荐工作流（最常用）

1. **先跑 TF卡导入（main.py）**：把最新点云复制到本地 `DST`，必要时重命名。
2. **再跑 LidarME 自动化（auto.py）**：  
   - 通常选“自动识别 + 执行导入”  
   - 如果任务已导入过：选“手动 pending + 跳过导入”
3. **最后跑 写入Excel（points2SLAM_Output.py）**：选择导出的 `points.csv` 和目标 Excel，写入对应 Sheet。

---

## 常见问题提示

- **手动 pending 为空且关闭自动识别**：auto.py 会提示 pending 为空并退出（这是预期行为）。
- **跳过导入步骤但任务列表里没有对应任务**：auto.py 会找不到任务行并跳过/退出，需要先导入或确认任务名称一致。
- **LidarME程序启动**：如果不事先启动程序，让程序调出点云处理程序，会导致打开的程序没有之前的点云记录，建议自己先启动LidarME